{
	"name": "gamea",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"homepage": "https://okasmin-Parsons-P5Party-SP25.github.io/GameA",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "mkdir -p dist && cp src/index.html dist/ && cp src/style.css dist/ && cp src/main.js dist/",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d dist"
	},
	"devDependencies": {
		"eslint-config-p5js": "^1.1.0",
		"gh-pages": "^6.3.0"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
