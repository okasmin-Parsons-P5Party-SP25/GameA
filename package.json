{
	"name": "gamea",
	"version": "1.0.0",
	"description": "",
	"type": "module",
	"homepage": "https://okasmin-Parsons-P5Party-SP25.github.io/GameA",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "mkdir -p dist && cp src/index.html dist/ && cp src/style.css dist/ && cp src/path.js dist/ && cp src/grid.js dist/ && cp src/main.js dist/ && cp -r src/images dist/",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d dist"
	},
	"devDependencies": {
		"eslint-config-p5js": "^1.1.0",
		"eslint": "^9.20.1",
		"gh-pages": "^6.3.0"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
